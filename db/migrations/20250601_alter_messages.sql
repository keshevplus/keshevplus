BEGIN;

ALTER TABLE messages
  DROP COLUMN IF EXISTS sent_at,
  DROP COLUMN IF EXISTS read_at,
  ADD COLUMN is_read BOOLEAN NOT NULL DEFAULT FALSE,
  ADD COLUMN previous_message_count INTEGER NOT NULL DEFAULT 0;

COMMIT;
